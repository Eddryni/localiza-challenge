import React from 'react';
import { Typography } from '@mui/material';

export default function SearchError() {
  return (
    <Typography color="error">Erro ao carregar sorvete.</Typography>
  );
};
